export const GRID_SIZE = 20;
export const SNAP_SIZE = 40; // Snap increment: matches NODE_WIDTH for easy alignment
export const CANVAS_SIZE = 8000;
export const CENTER_POS = CANVAS_SIZE / 2;

export const NODE_WIDTH = 40;
export const NODE_HEIGHT = 40;
export const NODE_RADIUS = 20;
export const MARRIAGE_DROP_Y = 40;
export const MARRIAGE_BAR_Y = NODE_RADIUS + MARRIAGE_DROP_Y;
export const GROUP_PADDING = 40;
export const GROUP_RADIUS = 10;

